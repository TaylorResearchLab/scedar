language: python
python:
  - "3.5"
  - "3.6"
# command to install dependencies
install:
  - pip install .
# command to run tests
script:
  - sh ./scripts/gen_baseline_images.sh && sh ./scripts/pytest_all.sh
